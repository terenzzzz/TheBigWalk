development:
  adapter: postgresql
  database: blank_sample_dev
  min_messages: warning
  
  # uncomment these lines if you are connected to postgres over the network stack
  # host: localhost
  # username:
  # password:

test:
  adapter: postgresql
  database: blank_sample_test
  min_messages: warning

  # uncomment these lines if you are connected to postgres over the network stack
  # host: localhost
  # username:
  # password:
