-# frozen_string_literal: true
= simple_form_for(@user) do |f|

  .form-inputs
    = f.input :avatar, label:"avatar"
    = f.input :name
    = f.input :description
    = f.input :mobile

  .form-actions
    = f.button :submit
